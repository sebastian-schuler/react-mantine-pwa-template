import { Center, Loader } from '@mantine/core';

function Loading() {
    return (
        <Center h={'100%'}>
            <Loader color='primaryAccent' />
        </Center>
    );
}

export default Loading;
